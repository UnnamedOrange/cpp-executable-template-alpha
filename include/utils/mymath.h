#pragma once

#include <type_traits>

namespace app::utils::mymath
{
    /**
     * @brief Calculate the sum of the two parameters.
     *
     * @param a1 One number.
     * @param a2 Another number.
     * @return int The sum of the two numbers.
     */
    int add(int a1, int a2);
} // namespace app::utils::mymath